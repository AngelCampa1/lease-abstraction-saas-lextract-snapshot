# Lextract Pristine Quality Sweep — Master Tracker

Goal: every part of Lextract must be functionally correct, visually consistent, tasteful, and
intuitive. Sellable to big clients. Verified via REAL local e2e (live OpenRouter). Iterate in
review/fix cycles until nothing remains. Sub-agent driven; cheap models where possible.
Treat prior sweeps as hints, never trust them.

Started: 2026-06-10. Owner: orchestrator agent. Update this file every session.

## Local environment (established)
- Frontend dev: `cd frontend && npm run dev` → http://localhost:3000 (boots clean; `.env.local` written, gitignored).
- Backend: Docker stack. Anonymous flow needs only OpenRouter + Postgres + MinIO + Redis.
- Real OpenRouter key is in `backend/.env` (gitignored, perms 600). Everything else local.
- DB: direct SQL via `NEON_DATABASE_URL` (psycopg). Migrations in `backend/neon/migrations/*.sql` (apply in order; stub `auth.user_id()` first).
- Storage: boto3 S3-compatible → MinIO via `R2_ENDPOINT_URL`.
- Auth: anonymous flow = pure DB, no Neon Auth. Authenticated routes need JWT (stub or DB-edit to test).
- Local stack compose: `backend/docker-compose.local.yml` (postgres + minio + redis + web + worker), bootstrap: `backend/scripts/local-bootstrap.sh`.

## Surface area (from inventory)
- Marketing: ~60 routes, many pSEO ([slug]) + ~15 hand-written. Home, pricing, about, sample-report, 5 SEO landings, resources hub, tools, calculators, fields, glossary, red-flags, etc.
- App (auth): /dashboard, /profile, /billing.
- Public-app: /upload, /processing/[id], /results/[id]  ← CORE PRODUCT FLOW.
- Auth: /login, /signup, /forgot-password, /reset-password.
- Backend API: auth, extractions, payments, user, tasks, leads, webhooks.
- Extract SDK: multi-pass orchestrator → confidence → red flags.

## Local stack — operational runbook (VERIFIED working 2026-06-10)
- Infra: `cd backend && docker compose -f docker-compose.yml -f docker-compose.local.yml up -d`
- One-time DB+bucket: `bash backend/scripts/local-bootstrap.sh`
- PORTS (host contention worked around): web=**8001** (host 8000 is taken by an ssh tunnel + stray python), postgres=**5433** (5432 taken). Internal container ports unchanged.
- MinIO needs `MINIO_KMS_SECRET_KEY` (set in compose.local) so SSE-S3 `AES256` uploads work.
- Frontend `.env.local` → `NEXT_PUBLIC_API_URL=http://localhost:8001/api/v1`. Restart `npm run dev` after env changes.
- Anonymous flow e2e (no Neon Auth needed): POST /auth/anonymous → POST /extractions/upload (X-Session-Token, multipart) → GET /status → GET /teaser.
- Test leases: `backend/tests/fixtures/real_lease_*.pdf`.
- get-session 404 in frontend is EXPECTED locally (no Neon Auth). Authed routes need JWT stub or DB edit.

## Methodology notes
- Full-page screenshots MISREPRESENT scroll-reveal pages (content sits opacity:0 until scrolled). Use viewport-sectioned captures + scroll, not fullPage, to judge marketing pages.

## Phases
- [x] P0  Local stack standup + first real anonymous extraction e2e (live OpenRouter). DONE 2026-06-10: 12pg Georgia lease → 124 fields, multi-pass corrections, 5 red flags, 4¢, 73s. Teaser returns real data.
- [~] P1  Core product flow polish (upload → processing → results → export). Highest sell-impact.
  - [x] Upload happy path (LIVE e2e: dropzone → live OpenRouter → processing → teaser). VERIFIED.
  - [x] Upload error path (LIVE: wrong file type → "Only PDF files are accepted." role=alert, focus returns to dropzone). Dropzone has drag/disabled/drag-active states, 50MB limit, aria-label, single-file guard — pristine.
  - [x] Processing page (LIVE: step progress, time estimate, notification opt-in, cancel/upload-another, only-expected console 404s). FailedState component has clear cause list + retry. Good.
  - [x] Teaser (LIVE: humanized enums, ranked preview fields, confidence chart, red-flag badge, locked-category grid, guest checkout CTA). F8 count bug fixed. Pristine.
  - [x] Guest checkout CTA (CODE REVIEW: a11y aria-describedby/invalid/role=alert, disabled/loading states, non-blocking analytics, Stripe trust signal). MINOR taste nit logged (F9-divider).
  - [x] Full results view / paid deliverable (CODE REVIEW: search, expand/collapse-all, split PDF + source highlight, edit history, dismissible success banner, sticky export/red-flag panel, graceful loading/error/stale-cache). Well-built. LIVE VERIFY **BLOCKED** — see B1-auth: frontend route-guards paid results behind a logged-in JWT user (anonymous-paid is redirected to /login by design, results-content.tsx:173); local Neon Auth is non-functional (404), so the authed render can't be reached locally. Backend path confirmed correct: get_extraction_full serves owned+paid records.
  - [ ] Export panel live download (xlsx/docx/pdf) — **BLOCKED on B1-auth** (paid+authed session unreachable locally). Export *content* verified via real ExcelExporter run (F8). Live download UX to verify once an auth path is provided.
  - [x] F9-divider polish — DONE: guest-checkout divider "- already have an account? -" → "Already have an account?" (dropped redundant literal hyphens that doubled the border-line divider). Committed.
- [~] P2  Marketing hand-written pages: visual + UX + copy (humanizer + third-grade) sweep.
  - [x] Home (/) — LIVE visual pass at 1440px: hero, 3-step, sample-output card, red-flag examples, footer all tasteful + consistent (pill buttons, color-coded category pills, confidence badges mirror the real teaser). Footer headings only LOOKED ghosted (translucent sticky nav overlap); computed opacity=1, dark teal. No fixes needed.
  - [x] Pricing (/pricing) — LIVE: hero, 3 value cards, 3 price cards, comparison table. Pack math verified (5-pack \$65=\$13/lease=13% off; 10-pack \$120=\$12/lease=20% off). FIXED F10 (pack cards mislabeled total as "per lease extraction").
  - [x] SEO landings (5): ai-lease-abstraction, automated-lease-abstraction, lease-abstraction-software, lease-abstraction-services, lease-extraction-software — source-level defect hunt. All pricing via PRICING constant, field count 126, red flags 20, all internally consistent; related-page links resolve to real routes. FIXED F11 (heading/content mismatch on ai-lease-abstraction). One low-pri smell noted (hardcoded 126/20 alongside PRODUCT_*_COUNT constants on ai-lease-abstraction — currently correct, fragile only).
  - [x] about, sample-report — pristine. sample-report sample figures are fully internally coherent ($28.50/RSF×5,000=$142,500/yr=$11,875/mo; deposit $35,625=3mo; TI $75k=$15/RSF; pro-rata 5,000/35,000=14.3%; parking 15=3/1,000×5,000). No defects.
  - [x] Hub index pages (16: calculators/case-studies/clauses/features/fields/for/glossary/industries/integrations/lease-types/locations/property-types/red-flags/templates/use-cases/workflows) — count-drift triage: grepped all numeric product-fact claims; every "126 fields" and "20 red flags/categories/rules" matches canonical. No hardcoded "all N <items>" claims that could mismatch data-array lengths → hubs derive list counts from `.length` (robust). No correctness defects in this class.
  - [ ] about/angel-campa, tools (+lease-comparison), faq — remaining (bespoke copy read). Live VISUAL pass of hubs/templates folds into P3.
- [ ] P3  pSEO template families: visual consistency + correctness across representative slugs.
- [ ] P4  Auth + app shell + dashboard/profile/billing.  **BLOCKED on B1-auth** — all authed surfaces unreachable via real login locally.
- [~] P5  Cross-cutting: responsive (mobile/desktop), dark mode, a11y (axe), keyboard, empty/error/loading states, pill-button canon, fonts/spacing consistency.
  - [x] Pill-button canon — CLEAN app-wide (see F14): Button base + all variants pill; raw <button>/link-button grep found zero non-pill button-styled elements; fixed the one violation (segmented tabs control).
  - [x] Landing (/) @ 390px mobile — hero, value cards, header/hamburger render cleanly; strong hierarchy; no overflow/clipping. Minor watch (not a defect): floating "Chat with Lextract" widget overlaps card body text at one scroll position — standard floating-widget behavior, scrollable past.
  - [x] Landing (/) dark mode @ mobile — renders correctly: dark surfaces, teal accent headline, good body-text contrast; multicolor logo kept on a white chip for legibility (intentional). Tailwind `.dark` variant applies correctly.
  - [x] Structural a11y pass (live, anonymous surfaces: /, /pricing, /results teaser) — CLEAN. DOM-heuristic scan (axe CDN blocked by app CSP, so used direct DOM checks): every page has exactly 1 h1, no skipped heading levels, `lang="en"`, zero images missing alt, zero buttons/links without an accessible name, zero inputs without a label. Teaser error-state (backend-down) also renders an accessible "Connection problem" card with Tracking ID + recovery action. NOTE: desktop header/footer text nav links measure ~20px tall (<24px WCAG 2.5.8) but that target-size rule exempts inline/nav text links and mobile collapses to a hamburger — not a defect.
  - [ ] Remaining: pricing/upload/teaser @ mobile + dark; desktop responsive edge widths; keyboard tab-order walkthrough; empty/error/loading states; font/spacing consistency across pSEO.
- B1-auth root cause CONFIRMED this session: `NEON_AUTH_BASE_URL=http://localhost:8000` resolves to the CapVeri FastAPI app (openapi title "CapVeri API"), which has no Neon Auth routes → `/get-session` + `/token` 404. Same root cause as F15. No local Neon Auth service exists (Neon Auth is a hosted identity product). User directive this session: "all testing should be local" — which rules out pointing at hosted/staging auth. Net: authed surfaces (rest of P1 + all P4) stay verified-by-tests+review-only until the user stands up a local auth backend or provides a local-resolvable NEON_AUTH_BASE_URL.
- [ ] P6  Functional/test hardening: vitest, pytest, sdk tests, build, typecheck, lint all green.
- [ ] P7  Final adversarial review cycles until nothing remains.

## Blockers (need user input)
- B1-auth [BLOCKER, 2026-06-10] Authenticated surfaces cannot be exercised via the real login flow on the local stack. The frontend proxies auth to `NEON_AUTH_BASE_URL`; `.env.local` stubs it to `http://localhost:8000/neondb/auth`, which the backend does NOT host (Better Auth signup/login → 404). The intended value (`.env.example`) is a HOSTED Neon Auth project (`https://<project-id>.neonauth…neon.build/neondb/auth`). Confirmed live: signup POST /api/auth/sign-up/email → 404 "Not found"; get-session → 404. Impact: blocks LIVE verification of the paid full-results view, export-panel downloads (rest of P1), and all of P4 (dashboard/profile/billing). The frontend gate is intentional (results-content.tsx:169-176 + 206-219): paid results require a JWT `user`; anonymous-paid is redirected to /login. Seed users (alice/bob in public.users) have NO auth credentials. Options surfaced to user: (1) provide dev/staging NEON_AUTH_BASE_URL + cookie secret for local login, (2) prod E2E creds (LEXTRACT_E2E_*) read-only against live, (3) tests+review only. Until resolved: deferring authed surfaces, proceeding with public surfaces (P2/P3 + public-page cross-cutting).

## Findings log (newest first)
- F18-teaser-skeleton-NOT-A-DEFECT [RESOLVED — NO BUG, infra] Spent prior cycles chasing an apparent "teaser stuck on loading skeleton" on /results/7d1e550b… that earlier rendered the full teaser. Root cause is purely environmental: the colima/Docker daemon crashed mid-session, SIGKILLing the backend stack (backend-web-1/worker exited 137), so the host-published :8001 (docker-compose.local.yml line 41 `8001:8000`) refused all connections — browser console showed `net::ERR_CONNECTION_REFUSED @ localhost:8001`. The product handled this CORRECTLY: instead of an infinite skeleton, the steady-state render is a clean "Connection problem / Check your connection and try again" error card with a Tracking ID and "Upload a document" recovery action (the skeleton was only the sub-second transitional state before the fetch error surfaced). After restarting colima + `docker compose -f docker-compose.yml -f docker-compose.local.yml up -d web worker`, :8001/health=200 and the teaser renders fully end-to-end: heading `real_lease_georgia_gov.pdf`, "Extraction complete. Here is your preview.", "Key Lease Terms — 5 of 126 fields shown", real data (Landlord GEORGIA BUILDING AUTHORITY, Lease Structure Gross). F12's 126-anchor holds live. METHODOLOGY NOTE: host curl to :8001 returning HTTP 000 is a real signal the backend is down, not a "host port quirk" — verify daemon/container health before suspecting the app. Error-state UX is good; no fix needed.
- F17-pricing-table-focus-ring [FIXED, LOW, a11y/keyboard] Same class as F16: the pricing comparison table is a correctly-built focusable scroll region (app/(marketing)/pricing/page.tsx:240, tabIndex=0 + role="group" + aria-label + mobile "swipe to compare" hint) but had no visible focus indicator. Added `rounded-lg outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50` so keyboard users see the region focus. tsc + eslint clean. Systematic sweep done: grep for onClick-on-div / role="button" / tabIndex=0 across components+app found ONLY these two focusable-scroll-region cases (dropzone F16, this table) lacking focus styling — both now fixed; no clickable-div anti-patterns exist (app uses real Buttons). Pricing page also visually verified clean @ 390px mobile: 3 tiers with correct discount math ($65 5-pack=13% off $75, $120 10-pack=20% off $150), feature list, FAQ, CTA — all pill, consistent.
- F16-dropzone-focus-ring [FIXED, LOW-MED, a11y/keyboard, commit 4ddcd963] The upload dropzone (components/upload/dropzone.tsx) is keyboard-operable (react-dropzone wires Enter/Space → file dialog; good aria-label) but had NO visible focus indicator — it relied on the browser default outline, inconsistent with the app's branded focus-visible:ring pattern (Button etc.). A keyboard user / 80-yo tabbing through got no clear "you are here" cue on the single most important control in the conversion flow. FIX: added `outline-none focus-visible:border-primary focus-visible:ring-[3px] focus-visible:ring-ring/50` matching the Button pattern. Verified live: tabbing/focusing the dropzone now shows a teal border + ring. 7/7 dropzone tests still pass; tsc clean. NOTE for deeper a11y pass: react-dropzone sets role="presentation" on the focusable root (the inner <input> is the labelled control) — known library quirk, left as-is.
- F15-auth-probe-404-noise [OPEN, MED, needs auth subsystem (B1-auth blocked)] LIVE on the anonymous teaser/results pages, the browser console shows red errors every load: `GET /api/auth/get-session 404` and `GET /api/auth/token 404` (plus a harmless dev-only tanstack query-devtools chunk 404). Two real concerns: (1) lib/api.ts:51-78 documents and assumes the token endpoint "Returns 401 when unauthenticated" and only swallows status 401 — any other status (incl. the observed 404) calls captureFrontendApiError → so unauthenticated probes get mis-reported to Sentry as "Auth token endpoint unavailable", which would bury real auth failures in false noise. (2) get-session/token are fired even on anonymous pages (better-auth React client auto-probes session on mount), so a discerning technical buyer opening devtools on the public site sees red 404s. The proxy is a thin SDK passthrough (app/api/auth/[...path]/route.ts → auth.handler()), so the 404-vs-401 status is decided by the neon-auth/better-auth config/version. FIX PATH (do during P4 auth pass with the auth backend running): confirm the real unauthenticated status from better-auth; either (a) make lib/api.ts treat 404 the same as 401 (silent, no Sentry capture) if the SDK legitimately 404s, and/or (b) suppress the client's auto session-probe on public/anonymous routes. Do NOT blind-edit the proxy — verify against a live authenticated session first.
- F14-tabs-pill-canon [FIXED, LOW, design-canon, commit e6bedfcd] The results-view segmented control (components/ui/tabs.tsx → the "Data"/"PDF" toggle in components/results/split-results-view.tsx) used rounded-lg list + rounded-md active indicator. CLAUDE.md design canon requires pill geometry on segmented/toggle controls. Swapped the default-variant list and trigger to rounded-full; the underline `line` variant keeps rounded-none (unaffected, vertical/line usage not present). tsc + eslint clean. Could not screenshot live — split-results-view is behind auth (B1-auth blocker); change is a deterministic class swap on a horizontal 2-tab control. Sweep result: grep for raw <button>/link-buttons with non-pill radii found ZERO — every other CTA already routes through the pill Button base. Pill canon now clean across the app.
- F13-processing-time-overstated [OPEN, MED, trust/UX — needs product decision] LIVE e2e: all 3 real leases (12-19 pages) finished in ~40-48s wall-clock through the full local pipeline (upload → extract via OpenRouter/Gemini → 3-pass validation → scoring). Yet the canonical processing-time claim is "5-15 minutes" everywhere: in-app live estimate (data/public-knowledge/app.ts:19-22, the message a waiting user literally watches), and site-wide marketing (data/public-knowledge/marketing.ts:47-50 canonicalRange/headline/detailed/vsManual, plus ~9 marketing pages via PROCESSING_TIME). That's a ~10-20x overstatement. The in-app one is the worst UX: a user told "5-15 minutes" may tab away and miss a sub-minute result, or read the product as slow/unsure. NOT auto-fixed — the canonical number is a factual+competitive claim and 3 local runs are not enough to set it: production runs on a shared worker (celery concurrency 4, so concurrent uploads queue), max doc is larger (50-page limit), and validation cost scales with pages/fields. RECOMMENDATION: pull p50/p95 processing duration from PostHog production telemetry, then retune the canonical value (likely "under 2 minutes" for the in-app estimate at minimum). Owner decision needed before editing the site-wide claim; the in-app estimate (app.ts) could be tightened independently since it is pure UX, not an SEO/comparison claim. Any change must pass humanizer + third-grade copy gates.
- F12-teaser-field-count-drift [FIXED, HIGH, data-integrity/brand] LIVE e2e (3 real leases through the running local stack, worker calling OpenRouter/Gemini): the teaser advertised a field total equal to however many keys the model happened to return, not the canonical 126. The georgia_gov lease rendered "5 of 124 fields shown", "124 fields across 16 categories", "Unlock all 124 fields for $15" while every marketing surface says 126 — and the confidence chart summed to 124, with "119 remaining" disagreeing with the locked-category sum (121). Root cause: `total_field_count = len(extracted_data)` and the confidence distribution counted only emitted keys (extractions.py:568, _compute_confidence_distribution). A discerning buyer (or an 80-yo) sees "you promised 126 but show 124" and loses trust. FIX: anchor `total_field_count` to `build_lextract_registry().field_count` (126) and add a `total_fields` param to `_compute_confidence_distribution` that folds never-emitted fields into not_found, so the four tiers always sum to 126. Verified LIVE in-browser: georgia now reads "5 of 126", "126 across 16 categories", "Unlock all 121 remaining", "Unlock all 126 fields for $15", chart 37+18+5+66=126. TDD: 2 new unit tests + 1 endpoint test added; 3 stale tests that encoded the old len()-based contract updated; full suite green (1 pre-existing macOS-path failure unrelated). ruff/black/mypy clean; extractions.py per-file coverage 95%. NOTE: uvicorn --reload does not fire on macOS Docker bind mounts — web container needed a manual `docker restart` to pick up the change.
- F11-heading-mismatch [FIXED, LOW-MED, clarity] On /ai-lease-abstraction, the section heading "How Lextract Confidence Scoring Works" (ai-lease-abstraction/page.tsx:238) sat directly above a method-COMPARISON table (Manual vs ChatGPT vs Lextract on cost/time/fields/review-signal) — not an explanation of confidence scoring. The heading promised one thing and delivered another; a discerning reader scanning headings would be misled. FIX: renamed heading to "How Lextract Compares to Manual Work and ChatGPT" and added a one-line bridge sentence ("Here is how the three methods compare.") so the confidence paragraph leads naturally into the comparison. Pure JSX text change; no type/test impact; plain-language (passes third-grade gates).
- F10-pricing-label [FIXED, MED, conversion-impact] On /pricing, the multi-pack price cards mislabeled the pack total as a per-lease rate: the 5-Pack card showed "$65 / per lease extraction / 5-Pack" and the 10-Pack "$120 / per lease extraction / 10-Pack". $65 and $120 are the PACK totals, not per-lease prices — a careful buyer (and certainly an 80-yo) would read "$65 per lease extraction" as $65/lease and infer the 5-pack costs $325. Root cause: the sublabel "per lease extraction" was hardcoded for all three tiers in the price-callout map (pricing/page.tsx:189-191). FIX: thread `perLease` (15/13/12, already in PRICING) into each tier and render single→"per lease", packs→"$13 per lease"/"$12 per lease". Now the big total stays accurate and the per-lease figure reinforces the discount. Verified live: "$65 / $13 per lease / 5-Pack", "$120 / $12 per lease / 10-Pack". tsc clean; no test asserted the old string; copy is data-label-plain (passes third-grade gates trivially).
- F9-divider [FIXED, MINOR, taste] Guest-checkout-cta divider label read "- already have an account? -" — the literal hyphens duplicated the border-line `<div className="w-full border-t" />` divider, reading as visual noise. Fixed to "Already have an account?" (plain, third-grade level, no transformation needed for a 4-word divider). frontend/components/results/guest-checkout-cta.tsx:220. No test referenced the string; text-only change, committed.
- F8-count [FIXED, MED, sell-impact] Field-count inconsistency across buyer surfaces: the teaser showed "5 of 124 fields", "124 fields across 16 categories", "Unlock all 124" — but the Confidence Distribution chart summed to 125 ("of 125 fields"), and all three paid exports (xlsx/docx/pdf) inflated MEDIUM by one. Root cause: the synthetic `_overall` aggregate meta-key (tier="medium") stored in `confidence_scores` was counted as if it were a field. `total_field_count = len(extracted_data)` (no `_overall`) disagreed with every tier-count loop that iterated `confidence_scores.values()`. FIX: skip underscore-prefixed meta keys — `_compute_confidence_distribution` + `_compute_overall_confidence` (teaser/results) now iterate `.items()` and `continue` on `name.startswith("_")`; added shared `field_confidence_scores()` helper to `exports/utils.py` wired into the summary-count loops of pdf/excel/word exporters. No real schema field name starts with `_` (verified against all 126 in lextract_field_schema.json). VERIFIED LIVE: fresh browser upload (real_lease_georgia_gov.pdf → live OpenRouter extraction 1fd9f6b2) — teaser chart now reads 32 High / 20 Medium / 6 Low / 66 Not-in-lease = 124, every denominator "of 124 fields", matching total_field_count. Real ExcelExporter run on the stored data: OLD MEDIUM=21/sum=125 → NEW MEDIUM=20/sum=124==field count. 1456 backend tests green (1 pre-existing unrelated Windows-path failure in test_field_editing.py), ruff/black/mypy clean. Caught by adversarial code review after the teaser-only fix missed the paid export surfaces.
- P1 core-flow LIVE e2e [DONE 2026-06-10] Real browser upload → processing → teaser verified end-to-end: dropzone upload of real_lease_georgia_gov.pdf triggered a live OpenRouter extraction (~60s), auto-redirected /upload → /processing/[id] → /results/[id]. Processing page: clean step progress, time estimate, notification opt-in, cancel/upload-another actions; only expected console 404s (tanstack devtools, get-session=no Neon Auth). Teaser: humanized enums confirmed in the live DOM (Lease Structure: Gross, Escalation Type: Stepped), proper name GEORGIA BUILDING AUTHORITY + prose untouched, no raw tokens — F7-enum now proven in-browser, not just via API.
- F7-enum [FIXED, MED, sell-impact] Raw enum/snake_case values rendered unpolished on teaser AND full results AND exports: `gross`, `stepped`, `not_unreasonably_withheld`, `pro_rata_allocation`. FIX: added `humanize_enum_value` to the shared `field_text.py` chokepoint, wired into `normalize_field_value` (teaser) + `exports/utils.format_value` (exports); mirrored `humanizeEnumValue` in frontend `formatFieldValue`. Rule is deliberately tight — only a bare lowercase token, optionally underscore-joined (`^[a-z]+(_[a-z]+)*$`), is title-cased; prose, proper names, numbers, dates, and filenames (`my_lease.pdf`) are left untouched. (Tightened after the first loose rule mangled the export filename into `My Lease.Pdf` — caught by `test_excel_export::test_summary_sheet_has_filename`.) VERIFIED LIVE: teaser API for 61a0ea6d now returns `Lease Structure: Gross` / `Escalation Type: Stepped` (proper name `GEORGIA BUILDING AUTHORITY` + prose `Two (2) additional…` untouched); real ExcelExporter run on the stored data shows `Gross` / `Stepped` / `Not Unreasonably Withheld` / `Pro Rata Allocation`, zero raw tokens, filename `real_lease_georgia_gov.pdf` unmangled. Per-file coverage 100% on both touched backend files; 58 frontend + 252 backend tests green; ruff/black/mypy/tsc/eslint clean.
- F7-numeric [OPEN, MED, EXTRACTION-LAYER — not display] `Holdover Rate: 1.0` / `Profit Sharing Percentage: 1.0` look unit-less on the surfaces, but the root cause is extraction semantics, NOT formatting. Evidence from 61a0ea6d `source_text`: profit_sharing 1.0 came from "if the proposed rental rate … is greater … such excess … deemed additional rent" → 1.0 means **100% as a fraction**, so naively appending `%` would render a misleading "1.0%". holdover_rate 1.0 came from "Tenant shall be a tenant at sufferance …" which states **no multiplier at all** → 1.0 is a defaulted/low-confidence guess. A schema-`data_type`-aware display formatter (percentage→`%`, currency→`$`, holdover number→`×`) is necessary polish but INSUFFICIENT and actively risky alone: it would format wrong/hallucinated values confidently. Correct fix lives in `packages/extract-sdk` normalization+validation (fraction-vs-percent canonicalization; "not stated" must beat a defaulted number). Deferred to a dedicated SDK pass (P6 functional/extraction hardening) with its own TDD against the SDK. Do NOT ship a bare display-suffix hack.
- F6 [FIXED, MED, sell-impact] Teaser backfill surfaced weak preview cards: for real extraction 61a0ea6d only `landlord_legal_name` of the 5 primary TEASER_FIELDS was populated, so backfill (document order) filled the preview with three boolean "No" flags + unit-less "1.0". FIX: `_build_teaser_fields` now orders primary → curated `TEASER_BACKFILL_FIELDS` (rent/term/size/structure economics) → other content text → booleans LAST. VERIFIED LIVE: preview went from `Landlord / Audit Rights:No / Auto Renewal:No / Has Guaranty:No / Holdover Rate:1.0` to `Landlord / Lease Structure:gross / Escalation Type:stepped / Renewal Terms:Two (2) additional one (1) year periods / Suite:Balcony Level`. (Was logged as F3.)
- F5 [FIXED, HIGH, sell-impact] Placeholder tokens (`{NAME OF TENANT}`, `insert …`) AND list/bool values leaked on the PAID surfaces too, not just the teaser: the full results page (frontend `formatFieldValue`) and the server-generated exports (xlsx/docx/pdf via `format_value`) rendered them verbatim. Root cause: teaser sanitization (F1/F2) lived only in the teaser path. FIX: extracted one shared backend helper `app/services/field_text.py` (`is_template_placeholder`/`clean_text_value`/`normalize_field_value`), reused by the teaser endpoint and `exports/utils.format_value`; mirrored the rule in frontend `types/extraction.ts:formatFieldValue`. VERIFIED LIVE: regenerated xlsx export for real extraction 61a0ea6d (template lease) — 0 leaked tokens, Tenant Name now "Not found" (Not-found cells 54→60, the 6 tokens converted). Frontend transform proven by 7 new `formatFieldValue` unit tests (the post-payment page itself is route-guarded behind Neon Auth, unreachable in local anon flow — server export is the conclusive surface). F1/F2 (teaser) committed in eba8385d; F5 extends the same fix to paid surfaces.
- F4 [FIXED, HIGH, prod-bug] Cloudflare Turnstile `challenges.cloudflare.com` was blocked by CSP. CONFIRMED WIRED: `turnstile-field.tsx` injects `challenges.cloudflare.com/turnstile/v0/api.js` and the widget is used across lead-magnet gates, calculators, email gates, exit popups, and feedback (`lib/public-form-abuse.ts` + 13 components/routes). Without the CSP origin the script + iframe + siteverify connect would all be blocked in production, silently breaking abuse protection on every public form. FIX (committed eba8385d): added `https://challenges.cloudflare.com` to `next.config.ts` script-src, frame-src, AND connect-src (verified present at lines 201/208/211). Not dev-gated — applies in prod.
- F3 [SUPERSEDED by F6] Teaser "Key Lease Terms" selects weak fields. Fixed by the F6 `_build_teaser_fields` ranking rewrite.
- F2 [SUPERSEDED by F5] Teaser showed raw template-placeholder tokens. Fixed by the shared `is_template_placeholder`/`clean_text_value` chokepoint (F5), which also covers the paid surfaces.
- F1 [SUPERSEDED by F5] Boolean fields rendered as Python `False`/`True`. Fixed by `normalize_field_value` (Yes/No) reused across teaser + exports (F5).
- INFO: Bottom-left "N" badge and tropical-image circle in dev screenshots are DEV-ONLY tooling (`<nextjs-portal>` Next.js dev overlay + TanStack Query Devtools `tsqd-open-btn-container`). They do NOT ship to production. The real Feedback button is `rounded-full bg-primary` (pill-canon, aria-label "Send feedback") — fine. Prior sweep findings about these were false positives. Methodology: dev overlays pollute screenshots; account for them.

## Decisions / notes
- 2026-06-10: User supplied real OpenRouter key; all other infra local. Confirmed via /goal.
